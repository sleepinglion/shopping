<div class="row">
	<h3 class="col-xs-12 col-sm-6"><%= @controller_name %> <%= actionNameChange controller.action_name %></h3>
	<div class="col-xs-12 col-sm-6">
		<%=form_tag admin_gene_categories_path,:method=>:get,:class=>"form-search pull-right" do %>
		<%= render :partial => 'layouts/admin/search_form' %>
		<% end %>
	</div>
</div>
<div class="table-responsive">
	<table class="table table-bordered table-striped">
		<colgroup>
			<col style="width:80px;" />
			<col />
			<col />			
			<col style="width:80px;" />
			<col style="width:100px;" />
			<% if can?(:update, GeneCategory) || can?(:delete, GeneCategory) %>
			<col <%= raw manage_width(GeneCategory) %> />
			<% end %>
		</colgroup>
		<thead>
			<tr>
				<th><%= t(:label_number) %></th>
				<th><%= t(:label_title) %></th>
				<th><%= t(:label_sub_title) %></th>				
				<th><%= t(:label_flag) %></th>
				<th><%= t(:label_created_at) %></th>
				<% if can?(:update, GeneCategory) || can?(:delete, GeneCategory) %>
				<th><%= t(:label_manage) %></th>
				<% end %>
			</tr>
		</thead>
		<tbody>
			<% unless @gene_categories.empty? %>
			<% @gene_categories.each do |gene_category| %>
			<tr>
				<td><%= link_to gene_category.id,admin_gene_category_path(gene_category) %></td>
				<td class="mw"><%= link_to gene_category.title,admin_gene_category_path(gene_category) %></td>
				<td><%=gene_category.sub_title %></td>
				<td><%= raw flagToString(gene_category.enable) %></td>
				<td><%= gene_category.created_at.to_date %></td>
				<% if can?(:update, GeneCategory) || can?(:delete, GeneCategory) %>
				<td> <% if can? :update, gene_category %>
				<%= link_to edit_admin_gene_category_path(gene_category),:class=>"btn btn-default" do %> <i class="glyphicon glyphicon-pencil"></i> <%= t(:link_edit) %>
				<% end %>
				<% end %>
				<% if can? :delete, gene_category %>
				<%= link_to admin_gene_category_path(gene_category), :method => :delete, :data => { :confirm => t(:delete_confirm) },:class=>"btn btn-danger" do %> <i class="glyphicon glyphicon-trash"></i> <%= t(:link_delete) %>
				<% end %>
				<% end %> </td>
				<% end %>
			</tr>
			<% end %>
			<% else %>
			<tr>
				<td colspan="5"><%= t(:no_data)%></td>
			</tr>
			<% end %>
		</tbody>
	</table>
</div>
<div class="row">
	<div class="col-xs-6 col-sm-3 col-md2">
		<% if can? :create, GeneCategory %>
		<%= link_to new_admin_gene_category_path,:class=>"btn btn-primary" do %>
		<i class="glyphicon glyphicon-plus"></i> <%= t(:link_new) %>
		<% end %>
		<% end %>
	</div>
	<%= paginate @gene_categories %>
</div>
